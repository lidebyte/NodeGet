[package]
name = "nodeget-lib"
version = "0.0.1"
edition = "2024"

[dependencies]
miniserde = { version = "0.1.45", default-features = false, features = ["std"], optional = true }
serde = { version = "1.0", optional = true, features = ["derive"] }
serde_json = { version = "1.0", optional = true }

[features]
for-agent = ["miniserde"]
for-server = ["serde", "serde_json"]