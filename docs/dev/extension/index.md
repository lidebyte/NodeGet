# 扩展开发

## 优秀插件汇总

占位下，优秀的插件会被整理一份到这里。

希望有开发者实现下面的插件：

### 进程管理器

- 灵活运用 Exec 任务执行 `ps`、`kill` 命令

### iptables 管理器

- 灵活运用 Exec 任务执行 `iptables` 命令

### Docker 管理器

- 灵活运用 Request 任务请求本地 Docker Unix Socket
